in the polls page, put the search box, filters and sorters on one line. all elements must have the same height as the search bar input.
they should stack only on resolutions of tablet/smartphone.  
put the "n" of polls number directly in the title "x polls open to you" and remove the copy "n open polls from the toolbar.  
remove from the sorting select the label, and prefix every option with "sort by: "
"sort by" select should be localized in every language (every option).
the scope limited to /poll/list and its existing controls